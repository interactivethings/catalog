[build]
  publish = "docs/build"
  command = "yarn build"

[build.environment]
  NODE_VERSION = "10"
  YARN_VERSION = "1.13.0"
  YARN_FLAGS = "--frozen-lockfile"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
